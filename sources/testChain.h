#include "Blockchain.h"
class testChain
{
private:
    /* data */
public:
    testChain(/* args */);
    ~testChain();
    int testHashChain(); //use it as a test suite for first codes to run through 
};